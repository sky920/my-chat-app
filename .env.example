# ===== 本地开发 =====
# DeepSeek API Key（在 https://platform.deepseek.com 申请）
# 本地开发时直接调用 DeepSeek API，Key 会暴露在前端（仅本地使用）
VITE_DEEPSEEK_API_KEY=sk-your-key-here

# ===== GitHub Pages 部署 =====
# GitHub Pages 是纯静态托管，不支持服务端函数（/api/chat 路由不存在）。
# 因此需要在构建时把 Key 注入前端 bundle：
#   1. 仓库 Settings → Secrets and variables → Actions → New repository secret
#   2. 名字填 VITE_DEEPSEEK_API_KEY，值填你的 sk-... Key
#   3. 在 GitHub Actions 工作流中读取该 secret 作为环境变量后再 build：
#      env:
#        VITE_DEEPSEEK_API_KEY: ${{ secrets.VITE_DEEPSEEK_API_KEY }}
# 注意：Key 会被打包进最终的静态 JS 文件中，建议仓库设为私有。

# ===== Vercel / Cloudflare Pages（Serverless Functions）部署 =====
# 这类平台支持 /api/chat 路由，API Key 可以只放在服务端：
#   在项目 Settings → Environment Variables 中设置：
#   DEEPSEEK_API_KEY=sk-your-key-here
# 注意：不要加 VITE_ 前缀，确保 Key 只在服务端可用，前端不可见。
# 若同时设置了 VITE_DEEPSEEK_API_KEY，会优先直连（跳过服务端代理）。
